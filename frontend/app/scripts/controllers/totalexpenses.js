'use strict';

/**
 * @ngdoc function
 * @name frontendApp.controller:TotalexpensesCtrl
 * @description
 * # TotalexpensesCtrl
 * Controller of the frontendApp
 */
angular.module('frontendApp')
  .controller('TotalexpensesCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
